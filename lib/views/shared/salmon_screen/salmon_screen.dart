import 'package:flutter/material.dart';

class SalmonScreen extends StatelessWidget {
  const SalmonScreen({
    required this.widget,
    super.key,
  });

  final Widget widget;

  @override
  Widget build(BuildContext context) {
    return widget;
  }
}
